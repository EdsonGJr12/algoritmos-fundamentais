const { somar } = require('../algoritmos-fundamentais');

test('Somar 1 + 2 + 3 deve resultar 6', () => {
    const resultado = somar(1, 2, 3);
    expect(resultado).toBe(6);
});

test('Somar 1 + 2 + 3 deve resultar 6', () => {
    const resultado = somar(1, 2, 3);
    expect(resultado).toBe(6);
});
